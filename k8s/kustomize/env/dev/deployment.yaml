apiVersion: apps/v1
kind: Deployment
metadata:
  name: litterrally-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: litterrally-dev
  template:
    metadata:
      labels:
        app.kubernetes.io/name: litterrally-dev
    spec:
      containers:
        - name: litterrally-dev-frontend
          image: 'ritchellegmp/litterrally-frontend:latest'
          imagePullPolicy: Always
          ports:
            - name: frontend-http
              containerPort: 80
              protocol: TCP
        - name: litterrally-dev-backend
          image: 'ritchellegmp/litterrally-backend:latest'
          imagePullPolicy: Always
          ports:
            - name: backend
              containerPort: 3333
              protocol: TCP
