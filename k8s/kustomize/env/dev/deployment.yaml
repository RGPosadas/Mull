apiVersion: apps/v1
kind: Deployment
metadata:
  name: mull-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mull-dev
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mull-dev
    spec:
      containers:
        - name: mull-dev-frontend
          image: 'ritchellegmp/mull-frontend:latest'
          imagePullPolicy: Always
          ports:
            - name: frontend-http
              containerPort: 80
              protocol: TCP
        - name: mull-dev-backend
          image: 'ritchellegmp/mull-backend:latest'
          imagePullPolicy: Always
          ports:
            - name: backend
              containerPort: 3333
              protocol: TCP
