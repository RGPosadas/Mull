import React from 'react';

/* eslint-disable-next-line */
export interface UiLibProps {}

export const UiLib = (props: UiLibProps) => {
  return (
    <div>
      <h1>Welcome to ui-lib!</h1>
    </div>
  );
};

export default UiLib;
